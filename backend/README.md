# Maverick Backend (NestJS)

Minimal NestJS scaffold.

Quick start

1. Install dependencies:

```bash
cd backend
npm install
```

2. Run in development mode:

```bash
npm run start:dev
```

The server listens on port 3000 by default.
